% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot.R
\name{embed_ggplot}
\alias{embed_ggplot}
\title{Embedding Plot Using ggplot2}
\usage{
embed_ggplot(
  coords,
  x = NULL,
  colors = NULL,
  color_scheme = NULL,
  num_colors = 15,
  alpha_scale = 1,
  limits = NULL,
  top = NULL,
  cex = 1,
  title = NULL,
  text = NULL,
  sub = NULL,
  equal_axes = FALSE,
  pc_axes = FALSE,
  xlim = NULL,
  ylim = NULL,
  show_axes = TRUE,
  NA_color = NULL,
  rev = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{coords}{Matrix of embedded coordinates, with as many rows as
observations, and 2 columns.}

\item{x}{Either a data frame or a column that can be used to derive a
suitable vector of colors. Ignored if \code{colors} is provided.}

\item{colors}{A single color or one color per observation.}

\item{color_scheme}{A color scheme. See Details. Ignored if \code{colors}
is specified.}

\item{num_colors}{Number of distinct colors to use in the palette, when
\code{x} is a numeric vector, on the assumption that the palette is continuous
(which it probably should be). Ignored if \code{x} is not a numeric vector. If
set to \code{NULL}, it will be set to \code{length(x)}.}

\item{alpha_scale}{Scale the opacity alpha of the colors, between 0 and 1.
Useful for increasing the transparency of points, especially with large
plots with lots of overlap.}

\item{limits}{The range that the colors should map over when mapping from a
numeric vector. If not specified, then the range of \code{x}. This is useful
if there is some external absolute scale that should be used. Ignored if
\code{x} is not a numeric vector.}

\item{top}{If not \code{NULL}, only this many finite values from a directly
supplied numeric \code{x} are displayed. Values are selected in decreasing order
with existing row order breaking ties.}

\item{cex}{Size passed to the ggplot2 point or text layer.}

\item{title}{Title for the plot.}

\item{text}{Vector of label text to display instead of a point. If the labels
are long or the data set is large, this is unlikely to be very legible, but
is occasionally useful.}

\item{sub}{Subtitle for the plot. Appears below the title.}

\item{equal_axes}{If \code{TRUE}, use a common coordinate range and equal physical
units on the X and Y axes.}

\item{pc_axes}{If \code{TRUE}, the \code{coords} are replaced by the first
two (unscaled) principal components, which should have the effect of
rotating the data (with a potential reflection) so the main variance aligns
along the X-axis. Should not have any other scaling effect.}

\item{xlim}{Vector of two numeric values to give the numeric extent of the
X-axis after any PCA rotation. Ignored if \code{equal_axes = TRUE}.}

\item{ylim}{Vector of two numeric values to give the numeric extent of the
Y-axis after any PCA rotation. Ignored if \code{equal_axes = TRUE}.}

\item{show_axes}{If \code{TRUE}, the axes, axis labels (and frame) are
displayed.}

\item{NA_color}{Color to use for \code{NA} values, which can arise if using
a factor column for \code{x} (or if any item in \code{colors} is \code{NA}). By default,
these points won't be displayed.}

\item{rev}{Logical indicating whether generated palettes or continuous scales
should be reversed. Explicit \code{colors} are unchanged.}

\item{verbose}{If \code{TRUE}, log messages to the console, mainly when
searching for a suitable color column in a dataframe.}
}
\value{
A ggplot object.
}
\description{
Plots embedded coordinates with ggplot2 and returns an ordinary ggplot
object, so callers can add their own themes, annotations, guides, or other
ggplot layers.
}
\details{
This is a small ggplot2 renderer for the same resolved coordinates and color
semantics as \code{\link[=embed_plot]{embed_plot()}}. It uses native ggplot2 discrete, continuous,
and identity scales. ggplot2 is an optional dependency and must be installed
to use this function.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
pca_iris <- stats::prcomp(iris[, -5], retx = TRUE, rank. = 2)
embed_ggplot(pca_iris$x, iris$Species, title = "iris PCA")
\dontshow{\}) # examplesIf}
}
